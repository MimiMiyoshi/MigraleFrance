# Supabase Configuration
# Supabaseのプロジェクト設定から取得できます
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# NextAuth Configuration
# 開発環境のURL
NEXTAUTH_URL=http://localhost:3000
# 以下のコマンドで生成: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-min-32-chars

# Database Configuration
# PostgreSQLの接続文字列
# 形式: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:password@localhost:5432/migrale

# Session Configuration
# 以下のコマンドで生成: openssl rand -base64 32
SESSION_SECRET=your-session-secret-key-min-32-chars

# Application Configuration
# 開発環境の場合は'development'を設定
# 本番環境の場合は'production'を設定
# テスト環境の場合は'test'を設定
NODE_ENV=development

# メール設定（オプション）
# SMTPサーバーの設定
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_FROM=noreply@example.com

# アプリケーション固有の設定
APP_NAME=Migrale
APP_URL=http://localhost:3000
SUPPORT_EMAIL=support@example.com

# セキュリティ設定
# CORS設定（カンマ区切りで複数指定可能）
ALLOWED_ORIGINS=http://localhost:3000,https://your-production-domain.com

# ログ設定
# ログレベル: debug, info, warn, error
LOG_LEVEL=debug

# 注意事項:
# 1. このファイルは環境変数のテンプレートです。実際の値は.envファイルに設定してください
# 2. 本番環境では、より強力なシークレットキーを使用してください
# 3. 開発環境と本番環境で異なる値を使用することを推奨します
# 4. 機密情報は安全に管理し、リポジトリにコミットしないでください 